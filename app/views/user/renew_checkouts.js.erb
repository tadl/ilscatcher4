/* render partial to update screen */
<% if !@user.error %>
  <% @userdetails = JSON.parse(cookies[:user]) %>
  $('#account-checkouts').html("<%= escape_javascript(render partial: 'checkouts_list') %>");

<% else %>
  /* not sure how this would happen */
<% end %>

