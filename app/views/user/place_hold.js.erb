<% if !@user.error %>
  $('#navbar').html("<%= escape_javascript(render partial: 'navbar') %>");
  $('.hold-button-<%= params[:id] %>').html('Hold successfully placed');
  <% puts @hold.inspect %>
  <% @hold['hold_confirmation'].each do |c| %>

  <% end %>
<% end %>
