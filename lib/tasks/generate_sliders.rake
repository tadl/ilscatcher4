desc "create lists for sliders and validate images"

task :generate_sliders => :environment do
  
  require 'open-uri'
  require 'mini_magick'

  lists = Settings.lists

  lists.each do |l|

  end

end
